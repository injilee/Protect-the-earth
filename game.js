'use strict';

const btn = document.querySelector('.startBtn');
const stopIcon = document.querySelector('.startBtn i');

startGame(){
    if(stopIcon.classList ){

    }
};

btn.addEventListener('toggle', startGame);